# Full Project Remaining Roadmap

Generated: 2026-05-21

Project root: `C:\Users\gabri\Desktop\Aegis\Tools\04 Runtime & Engine Tools\ChatBot`

This review covers the current ChatBot/Aegis workspace as a full local-first AI workspace platform: Aegis Core, Website backend/frontend, native desktop shell, VS Code extension, Visual Studio extension, release/update tooling, evidence contracts, provider accounts, model routing, memory, plugins, and docs.

## Current Verdict

The project is powerful and much better instrumented than it was at the start of the roadmap. It now has contract evidence for release packaging, rollback, packaged alpha scenarios, cross-client parity, release limitations, Core/Website ownership, backend modularization, frontend extraction, native desktop hardening, editor extension modularization, provider route safety, memory governance, plugin lifecycle, observability CI, distribution/update readiness, and alpha readiness polish.

It is not ready for a broad public release yet. It is closest to a private/controlled alpha with named limitations.

The hard release risk is not one failing test. The hard risk is scope control: the worktree is very mixed, the largest app/client/backend files still concentrate too much behavior, and the last full validation matrix still has skipped live smoke/package gates.

## Fresh Snapshot

Source inventory:

- Visible project files from `rg --files`: 844
- `git status --short` entries: 279
- Modified entries: 97
- Deleted entries: 1
- Untracked entries: 181

Latest evidence:

- Full release validation: `.aegis/release-evidence/20260521-110934/`
- Evidence ledger: `.aegis/evidence-ledger/20260521-111627/`
- Ledger status: `attention`
- Ledger runtime blockers: 0
- Latest Phase 29 editor evidence: `.aegis/editor-extension-modularization/20260521-081105/`
- Latest Phase 30 provider secret safety evidence: `.aegis/provider-secret-safety/20260521-082837/`
- Latest Phase 31 memory governance evidence: `.aegis/memory-governance/20260521-084434/`
- Latest Phase 32 plugin lifecycle evidence: `.aegis/plugin-lifecycle/20260521-090234/`
- Latest Phase 33 observability CI evidence: `.aegis/observability-ci/20260521-091230/`
- Latest Phase 34 distribution/update evidence: `.aegis/distribution-update/20260521-092140/`
- Latest Phase 35 alpha readiness polish evidence: `.aegis/alpha-readiness-polish/20260521-093558/`
- Latest Phase 36 live smoke RC evidence: `.aegis/live-smoke-rc/20260521-111524/`
- Latest Phase 37 commit scoping evidence: `.aegis/commit-scoping-github-handoff/20260521-111528/`
- Latest Phase 38 desktop smoke/package evidence: `.aegis/desktop-smoke-package-gates/20260521-111533/`
- Latest Phase 39 editor smoke/package decision evidence: `.aegis/editor-smoke-release-package-decision/20260521-111548/`

Latest full validation matrix status:

- Overall: `attention`
- Hard failures: 0
- Retryable gates: 1
- Skipped gates: 3
- Retryable: VS Code extension-host smoke, blocked by the `vscode-updating` mutex
- Skipped: Visual Studio package, release packaging dry run, final release manifest contract

Largest source hotspots:

| File | Lines | Why It Matters |
| --- | ---: | --- |
| `src/AegisChatApp.cpp` | 15,973 | Native UI behavior is still highly concentrated. |
| `website/frontend/src/App.tsx` | 11,071 | Web app shell owns too much routing/state/rendering. |
| `vscode-plugins/aegis-local-autopilot/extension.js` | 6,631 | VS Code activation still carries many command/runtime domains. |
| `website/backend/aegis_ai/agent.py` | 6,102 | Agent planning, routing, fallback, and prompt behavior remain broad. |
| `website/backend/aegis_ai/project_scaffolder.py` | 5,736 | Scaffolding needs smaller domain slices and fixture tests. |
| `website/backend/aegis_ai/storage.py` | 5,202 | Storage needs repository/service boundaries. |
| `src/AegisClient.cpp` | 4,457 | Desktop Core/backend client is still too broad. |
| `website/frontend/src/types.ts` | 4,361 | Type catalog needs domain split or generated clients. |
| `aegis-core/aegis_core/contracts.py` | 4,154 | Core contracts should be grouped or generated by domain. |
| `website/backend/aegis_ai/schemas.py` | 3,787 | Backend schema catalog should be split by API domain. |
| `website/backend/aegis_ai/main.py` | 3,766 | API assembly is improving but still heavier than ideal. |

## What Is Already In Good Shape

- Aegis Core has many runtime domains and tests in place.
- Website backend and frontend have broad test/build coverage.
- The ecosystem validation command exists and writes evidence.
- Release package dry-run, final manifest, preflight, apply/rollback, packaged scenarios, and cross-client parity evidence exist.
- The evidence ledger has zero runtime blockers and can attach Phase 35 evidence.
- Core/Website ownership is documented and contract-checked.
- Backend route-preview, provider route-safety, memory governance/export controls, plugin lifecycle controls, observability CI checks, distribution/update checks, alpha readiness polish checks, frontend provider-runtime helper, native Runtime Status presenter, and VS Code Core envelope helpers are extracted and guarded.
- Release notes and external alpha docs include known limitations.

## Roadmap From Here

### Completed Phase 30: Provider, Model Routing, And Secret Safety

Goal: make provider setup safe and understandable for real testers.

Progress:

- Added backend `ProviderRouteSafety` snapshots for local endpoints, API-key routes, CLI delegation, environment-profile routes, OAuth-pending routes, and unsupported routes.
- Added backend redaction for provider account, session, CLI bridge, readiness, and route-safety error text before snapshots leave the manager.
- Extended provider account tests for OS credential-store API-key routing, local no-secret routing, CLI no-token-import routing, and secret-like readiness error redaction.
- Extended frontend provider route-readiness cards to consume backend safety boundaries and redact secret-like card text.
- Added `evals/phase30-provider-secret-safety-contract.json`, `docs/PROVIDER_SECRET_SAFETY_EVIDENCE.md`, and `scripts/test-provider-secret-safety.ps1`.
- Wired `provider-secret-safety-contract` into `scripts/validate-ecosystem.ps1` and the evidence ledger.
- Generated ready evidence at `.aegis/provider-secret-safety/20260521-082837/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-082848/`.

Done when:

- Done for the first provider route-safety and secret-redaction slice.
- Remaining provider follow-up: live health checks, explicit launch-time consent, model mismatch/fallback tests, and provider outage coverage.

### Completed Phase 31: Memory, Privacy, And Governance Controls

Goal: make durable memory trustworthy.

Progress:

- Added Core-delegated Website backend support for memory governance, export, and controls while preserving the legacy local-memory fallback path.
- Added `GET /api/memory/governance`, `POST /api/memory/export`, and `POST /api/memory/controls`.
- Extended memory note responses with governance, privacy, control, delegation, and runtime metadata.
- Added local-only fallback governance, storage-boundary reporting, and secret-like redaction for legacy memory exports.
- Extended frontend memory API types and client helpers for governance, export, and control updates.
- Extended backend Core delegation tests for delegated governance/export/control behavior and offline legacy fallback redaction.
- Extended frontend API tests for memory governance, export, and controls.
- Added `evals/phase31-memory-governance-contract.json`, `docs/MEMORY_GOVERNANCE_EVIDENCE.md`, and `scripts/test-memory-governance.ps1`.
- Wired `memory-governance-contract` into `scripts/validate-ecosystem.ps1` and the evidence ledger.
- Generated ready evidence at `.aegis/memory-governance/20260521-084434/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-084545/`.

Done when:

- Done for the first memory governance/export/control slice.
- Remaining memory follow-up: prompt-injection scanning for files, tool output, copied text, imported docs, and future web/PDF imports, plus a broader tool-event ledger.

### Completed Phase 32: Plugin Ecosystem Productization

Goal: make plugins safe enough to ship.

Progress:

- Added productization plugin and ecosystem package lifecycle schemas for update, rollback, and safe-uninstall.
- Added checksum fields to productization plugin manifests and checksum mismatch validation to productization/ecosystem manifests.
- Added lifecycle routes for productization plugins and ecosystem packages: update, rollback, and uninstall.
- Stored previous manifests before update and uninstall actions so rollback can restore the prior manifest.
- Kept uninstall reversible with `safe_disable` instead of physical deletion.
- Added worker/ecosystem audit events for plugin and package lifecycle actions.
- Extended productization and ecosystem backend tests for checksum rejection, update, rollback, safe-uninstall, metadata, and audit visibility.
- Added `evals/phase32-plugin-lifecycle-contract.json`, `docs/PLUGIN_LIFECYCLE_EVIDENCE.md`, and `scripts/test-plugin-lifecycle.ps1`.
- Updated `docs/PLUGIN_ECOSYSTEM.md`, `docs/VALIDATION_MATRIX.md`, `docs/EVIDENCE_LEDGER.md`, `scripts/validate-ecosystem.ps1`, and the evidence ledger contract/script.
- Generated ready evidence at `.aegis/plugin-lifecycle/20260521-090234/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-090245/`.

Done when:

- Done for the first reversible plugin/package lifecycle productization slice.
- Remaining plugin follow-up: artifact-level signature verification, real package download/install staging, and rendered first-party plugin UI.

### Continuing Parallel Tracks

These are still high-priority slices, but they should not displace the numbered Phase 36 live smoke closure work:

- Backend modularization: continue splitting `agent.py`, `project_scaffolder.py`, `storage.py`, `schemas.py`, `contracts.py`, and `main.py` into focused service modules.
- Frontend product shell extraction: continue moving `App.tsx` route surfaces, provider panels, runtime status, validation, memory, release/update, and workspace UI into tested components/hooks.
- Native desktop shell/client split: continue extracting `AegisChatApp.cpp` panels and `AegisClient.cpp` Core API domains, then add desktop smoke evidence.
- Editor extension extraction and live smoke: continue splitting VS Code command registration, workspace scan, proposal/apply, validation, webview rendering, and retry VS Code/Visual Studio live smoke paths.

### Completed Phase 33: Observability, Evals, And CI

Goal: make quality reproducible.

Progress:

- Added `.github/workflows/aegis-validation.yml` with Windows CI jobs for Core tests, Website backend tests, Website frontend tests/build, desktop contract/build, VS Code unit/lint guards, Visual Studio validation guards, release/evidence contracts, and a manual full ecosystem matrix.
- Added `evals/phase33-observability-ci-contract.json`, `docs/OBSERVABILITY_EVALS_CI_EVIDENCE.md`, and `scripts/test-observability-ci.ps1`.
- Wired `observability-ci-contract` into `scripts/validate-ecosystem.ps1`.
- Added Phase 33 observability CI evidence to `docs/VALIDATION_MATRIX.md`, `docs/EVIDENCE_LEDGER.md`, `evals/phase16-evidence-ledger-contract.json`, and `scripts/test-evidence-ledger-contract.ps1`.
- Generated ready evidence at `.aegis/observability-ci/20260521-091230/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-091239/`.

Done when:

- Done for the first observability/evals/CI evidence slice.
- Remaining observability follow-up: uploaded CI artifacts, rendered route-quality dashboard, fallback reliability trend, token/cost drift trend, and scenario regression snapshots.

### Completed Phase 34: Distribution, Installer, Signing, And Auto-Update

Goal: make installation and updates real.

Progress:

- Added `docs/DISTRIBUTION_INSTALLER_UPDATE_EVIDENCE.md`, `evals/phase34-distribution-update-contract.json`, and `scripts/test-distribution-update.ps1`.
- Updated release packaging docs with the private-alpha distribution decision: portable ZIP packages for Core/Website/Desktop, VSIX packages for editor extensions, unsigned local-build disclosure, checksums required, and signed installer deferred until broad release.
- Verified `VERSION.json` and `release/version-manifest.json` component versions, package metadata, sizes, SHA-256 values, update policy, and release-relative artifact paths.
- Verified source and shipped updater markers for checksum validation, signature policy, backup, rollback, safe mode, security audit, downgrade protection, and `.aegis\updates` state.
- Verified local-only redacted Core diagnostics export through `/v1/alpha/diagnostics/export`.
- Ran non-mutating updater plans for all five components and confirmed every plan is `planned` with checksums required.
- Wired `distribution-update-contract` into `scripts/validate-ecosystem.ps1`, `docs/VALIDATION_MATRIX.md`, `docs/EVIDENCE_LEDGER.md`, `evals/phase16-evidence-ledger-contract.json`, and `scripts/test-evidence-ledger-contract.ps1`.
- Generated ready evidence at `.aegis/distribution-update/20260521-092140/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-092406/`.

Done when:

- Done for the current private-alpha portable ZIP and VSIX distribution evidence slice.
- Remaining distribution follow-up: installer technology selection, code signing, installer smoke evidence, client open-logs/diagnostics affordances, and live update/rollback on a clean install target.

### Completed Phase 35: Alpha Readiness Polish

Goal: make the first external build feel intentional.

Progress:

- Added `alpha_handoff_summary()` to Core alpha readiness and exposed it through the existing `/v1/alpha/readiness` payload.
- Labeled surfaces as Stable Alpha, Beta, Experimental, and Internal Only with default visibility rules.
- Added a narrow default alpha path: checksum verification, local Core launch, workspace selection, provider setup or local-only confirmation, local model setup, validate-only first workflow, checkpoint/rollback confirmation, diagnostics export, and feedback capture.
- Added `evals/phase35-alpha-readiness-polish-contract.json`, `docs/ALPHA_READINESS_POLISH_EVIDENCE.md`, and `scripts/test-alpha-readiness-polish.ps1`.
- Updated controlled alpha docs, first-run onboarding, external alpha release notes, known unstable surfaces, validation matrix, ecosystem validator, evidence ledger docs, and evidence ledger contract/script.
- Extended Core alpha readiness tests for the Phase 35 handoff summary.
- Generated ready evidence at `.aegis/alpha-readiness-polish/20260521-093558/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-093610/`.

Done when:

- Done for the first tester-facing alpha readiness polish slice.
- Remaining alpha follow-up: close live smoke/package skips, select signing/installer path, and group release commits before broad alpha.

### Completed Phase 36: Live Smoke Closure And Release Candidate Scoping

Goal: remove the skipped-gate ambiguity from release readiness.

Progress:

- Added `evals/phase36-live-smoke-rc-contract.json`.
- Added `docs/LIVE_SMOKE_RC_SCOPING_EVIDENCE.md`.
- Added `scripts/test-live-smoke-rc.ps1`.
- Wired `live-smoke-rc-contract` into `scripts/validate-ecosystem.ps1`.
- Added Phase 36 to validation matrix docs, external alpha release notes, external alpha report, known unstable surfaces, evidence ledger docs, and evidence ledger contract/script.
- Added live gate closure inventory for `desktop-smoke`, `vscode-smoke`, `visual-studio-package`, `release-package-dry-run`, and `update-plan-dry-run`.
- Separated Phase 36 evidence `status` from `release_candidate_status`, keeping the candidate at `attention` until live smoke and package gates are closed.
- Captured publish scope markers: `intentional_release_artifact_decision`, `worktree_scope_required`, `github_target_pending`, and `broad_alpha_blocked`.
- Generated fresh release validation evidence at `.aegis/release-evidence/20260521-095247/`.
- Generated ready Phase 36 evidence at `.aegis/live-smoke-rc/20260521-095750/`.
- Refreshed evidence ledger output at `.aegis/evidence-ledger/20260521-095800/`.

Done when:

- Done for the first release-candidate scoping slice.
- Latest Phase 36 refresh is `.aegis/live-smoke-rc/20260521-111524/live-smoke-rc-summary.md`.
- Remaining Phase 36 closure work: VS Code smoke, Visual Studio package/experimental smoke, release packaging, and installer/update evidence.

### Completed Phase 37: Commit Scoping And GitHub Handoff

Goal: make the worktree safe to publish.

Progress:

- Added `evals/phase37-commit-scoping-github-handoff-contract.json`.
- Added `docs/COMMIT_SCOPING_GITHUB_HANDOFF_EVIDENCE.md`.
- Added `scripts/test-commit-scoping-github-handoff.ps1`.
- Wired `commit-scoping-github-handoff-contract` into `scripts/validate-ecosystem.ps1`.
- Added Phase 37 to validation matrix docs, external alpha release notes, external alpha report, known unstable surfaces, evidence ledger docs, and evidence ledger contract/script.
- Added `commit_groups`, `publish_blockers`, `deleted_asset_decision`, `github_target`, `no_blind_stage`, `release_artifact_separation`, and `handoff_status` to the publish evidence.
- Kept publish handoff at `attention` until the owner confirms the GitHub target, resolves `website inspiration.png`, and stages by explicit commit group.
- Refreshed evidence at `.aegis/commit-scoping-github-handoff/20260521-111528/commit-scoping-github-handoff-summary.md`, `.aegis/evidence-ledger/20260521-111627/evidence-ledger-summary.md`, and `.aegis/release-evidence/20260521-110934/validation-summary.md`.

Done when:

- Done for the first commit scoping and GitHub handoff slice.
- Remaining Phase 37 closure work: confirm target repo, resolve the deleted asset decision, and stage scoped commits.

### Completed Phase 38: Desktop Smoke And Package Gate Execution

Goal: close the next runtime gate with real desktop/package evidence.

Progress:

- Added `evals/phase38-desktop-smoke-package-gates-contract.json`.
- Added `docs/DESKTOP_SMOKE_PACKAGE_GATES_EVIDENCE.md`.
- Added `scripts/test-desktop-smoke-package-gates.ps1`.
- Wired `desktop-smoke-package-gates-contract` into `scripts/validate-ecosystem.ps1`.
- Added Phase 38 to the validation matrix, external alpha release notes/report, known unstable surfaces, and evidence ledger docs/contract/script.
- Ran desktop runtime smoke with isolated app data and attached screenshots/log evidence.
- Ran `scripts\validate-ecosystem.ps1 -SkipSmoke -IncludeDesktopSmoke`; desktop smoke passed, with 0 hard failures, 0 retryable failures, and 4 intentionally skipped editor/package gates.
- Patched the Core runtime state writer to use unique temp state files with a Windows replace retry loop.
- Refreshed Phase 36, Phase 37, Phase 38, and evidence ledger evidence at `.aegis/live-smoke-rc/20260521-111524/`, `.aegis/commit-scoping-github-handoff/20260521-111528/`, `.aegis/desktop-smoke-package-gates/20260521-111533/`, and `.aegis/evidence-ledger/20260521-111627/`.

Done when:

- Done for the first desktop smoke/package execution slice.
- Remaining Phase 38 closure work: VS Code smoke, Visual Studio package, release-package dry run, and final release manifest mutation decisions.

### Completed Phase 39: Editor Smoke And Release Package Decision Execution

Goal: close the next skipped editor or package gate with explicit execution evidence.

Progress:

- Added `evals/phase39-editor-smoke-release-package-decision-contract.json`.
- Added `docs/EDITOR_SMOKE_RELEASE_PACKAGE_DECISION_EVIDENCE.md`.
- Added `scripts/test-editor-smoke-release-package-decision.ps1`.
- Wired `editor-smoke-release-package-decision-contract` into `scripts/validate-ecosystem.ps1`.
- Added Phase 39 to validation matrix docs, external alpha release notes/report, known unstable surfaces, and evidence ledger docs/contract/script.
- Fixed PowerShell native stderr handling in the validation wrappers so VS Code smoke progress output is logged instead of treated as a terminating exception.
- Ran VS Code extension-host smoke through Phase 39; it is now `retryable` because the VS Code test instance still holds the `vscode-updating` mutex.
- Ran `scripts\validate-ecosystem.ps1 -IncludeDesktopSmoke`; desktop smoke passed, VS Code smoke was retryable, and 3 package mutation gates remained skipped.
- Refreshed Phase 36, Phase 37, Phase 38, Phase 39, and evidence ledger outputs at `.aegis/live-smoke-rc/20260521-111524/`, `.aegis/commit-scoping-github-handoff/20260521-111528/`, `.aegis/desktop-smoke-package-gates/20260521-111533/`, `.aegis/editor-smoke-release-package-decision/20260521-111548/`, and `.aegis/evidence-ledger/20260521-111627/`.

Done when:

- Done for the first editor smoke/release package decision execution slice.
- Remaining Phase 39 closure work: rerun VS Code smoke after the update mutex clears, then close Visual Studio package and release package mutation gates.

### Phase 40: Package Mutation Decision And Artifact Gate Execution

Goal: close the next package mutation gate under an explicit release artifact decision.

Work:

- Confirm whether this checkout may mutate Visual Studio extension release artifacts, root release artifacts, or both.
- Run `visual-studio-package` with the release artifact decision attached, or classify it as owner-blocked.
- Run `release-package-dry-run` and `final-release-manifest-contract` only when root release mutation is intentional.
- Rerun VS Code extension-host smoke after the `vscode-updating` mutex clears.
- Refresh Phase 36, Phase 37, Phase 38, Phase 39, and evidence ledger outputs after the next gate status changes.

Done when:

- At least one package mutation gate is converted to passed, retryable, or owner-blocked evidence with the mutation scope recorded.

## Immediate Next 15 Tasks

1. Confirm whether the GitHub target is `MercyProductions/Aegis-AI` or another repo.
2. Decide whether the deleted `website inspiration.png` is intentionally removed.
3. Stage future commits by explicit group, never with a broad `git add -A`.
4. Start Phase 40 by confirming package mutation scope for Visual Studio and root release artifacts.
5. Rerun VS Code extension-host smoke after the `vscode-updating` mutex clears.
6. Run Visual Studio package or experimental-instance smoke with intentional release-artifact mutation.
7. Re-enable release packaging dry run and final manifest generation under a clean release-artifact decision.
8. Refresh Phase 36 RC scoping, Phase 37 handoff, Phase 38 package-gate evidence, Phase 39 editor/package evidence, and the evidence ledger after the next gate closes.
9. Review `.aegis/release-evidence/20260521-110934/validation-summary.md` before any publish attempt.
10. Keep package installs tied to checksum manifests and rollback guidance.
11. Split the next backend slice from `agent.py` or `storage.py`.
12. Split the next frontend slice from `App.tsx`.
13. Split the next native panel from `AegisChatApp.cpp`.
14. Split the next VS Code domain from `extension.js`.
15. Add prompt-injection scanning and a broader tool-event ledger before promoting durable memory to testers.

## Release Readiness Gates

Before broad release, require:

- Clean or intentionally scoped worktree.
- Full ecosystem matrix with no unexplained skipped gates.
- Native desktop smoke evidence.
- VS Code extension-host smoke evidence.
- Visual Studio package or experimental-instance smoke evidence.
- Release packaging dry run and update planner evidence.
- Provider secret redaction tests and route-safety evidence.
- Memory export/delete/governance evidence.
- Plugin lifecycle update/rollback/safe-uninstall evidence.
- Known limitations copied into release notes.
- GitHub target and commit scope confirmed.

## Recommended Commit Groups

1. Roadmaps and docs.
2. Evidence contracts and validation scripts.
3. Aegis Core runtime/contracts/tests.
4. Website backend route/service/repository work.
5. Website frontend shell/provider/memory work.
6. Native desktop source/build project changes.
7. VS Code extension source/package changes.
8. Visual Studio extension source/package changes.
9. Release/update/version tooling.

## Definition Of Done

A future phase is complete only when:

- The owning layer is clear.
- The change has focused tests.
- Existing validation still passes.
- Docs and contracts are updated.
- Security/privacy impact is checked.
- Release compatibility impact is known.
- Evidence is written to a stable `.aegis` folder.
- The next phase can continue without rediscovering project context.
